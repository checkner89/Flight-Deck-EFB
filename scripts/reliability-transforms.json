[
  {
    "file": "src/electron-main.mjs",
    "before": "  const currentVersion = app.getVersion();\n  let value = app.isPackaged && process.platform === 'win32'\n    ? { state: 'idle', currentVersion, configured: true, detail: 'GitHub-Updatekanal ist bereit.' }\n    : { state: 'manual', currentVersion, configured: false, detail: 'Updateprüfung ist nur in der installierten Windows-App aktiv.' };\n  const set = (patch) => { value = { ...value, ...patch, currentVersion, updatedAt: new Date().toISOString() }; };\n\n  if (app.isPackaged && process.platform === 'win32') {\n    autoUpdater.autoDownload = false;\n    autoUpdater.autoInstallOnAppQuit = true;\n    autoUpdater.on('checking-for-update', () => set({ state: 'checking', percent: 0, detail: 'GitHub Release wird geprüft.' }));\n    autoUpdater.on('update-available', (info) => {\n      const releaseName = info?.version || null;\n      const releaseNotes = normalizeReleaseNotes(info?.releaseNotes);\n",
    "after": "  const currentVersion = app.getVersion();\n  let value = app.isPackaged && process.platform === 'win32'\n    ? { state: 'idle', currentVersion, configured: true, detail: 'GitHub-Updatekanal ist bereit.' }\n    : { state: 'manual', currentVersion, configured: false, detail: 'Updateprüfung ist nur in der installierten Windows-App aktiv.' };\n  let operation = null;\n  const set = (patch) => { value = { ...value, ...patch, currentVersion, updatedAt: new Date().toISOString() }; };\n\n  if (app.isPackaged && process.platform === 'win32') {\n    autoUpdater.autoDownload = false;\n    autoUpdater.autoInstallOnAppQuit = false;\n    // Full downloads avoid differential blockmap failures across renamed releases.\n    autoUpdater.disableDifferentialDownload = true;\n    autoUpdater.on('checking-for-update', () => set({ state: 'checking', percent: 0, detail: 'GitHub Release wird geprüft.' }));\n    autoUpdater.on('update-available', (info) => {\n      const releaseName = info?.version || null;\n      const releaseNotes = normalizeReleaseNotes(info?.releaseNotes);\n"
  },
  {
    "file": "src/electron-main.mjs",
    "before": "    });\n    autoUpdater.on('update-not-available', (info) => set({ state: 'current', percent: 0, releaseName: info?.version || currentVersion, detail: 'FLYXORA ist aktuell.' }));\n    autoUpdater.on('download-progress', (progress) => set({ state: 'downloading', percent: Math.round(progress.percent || 0), detail: `Update wird heruntergeladen: ${Math.round(progress.percent || 0)} %` }));\n    autoUpdater.on('update-downloaded', (info) => set({ state: 'downloaded', percent: 100, releaseName: info?.version || null, releaseNotes: normalizeReleaseNotes(info?.releaseNotes) || value.releaseNotes || '', detail: `Version ${info?.version || ''} ist bereit. Neustart zum Installieren.`.replace(/\\s+/g, ' ').trim() }));\n    autoUpdater.on('error', (error) => set({ state: 'error', percent: 0, detail: `Update-Prüfung fehlgeschlagen: ${error.message}` }));\n  }\n\n  return {\n    status: () => ({ ...value }),\n    async check() {\n      if (!app.isPackaged || process.platform !== 'win32') return { ...value };\n      set({ state: 'checking', detail: 'GitHub Release wird geprüft.' });\n      await autoUpdater.checkForUpdates();\n      return { ...value };\n    },\n    async download() {\n      if (!app.isPackaged || process.platform !== 'win32') return { ...value };\n      if (!['available', 'error'].includes(value.state)) return { ...value };\n      set({ state: 'downloading', percent: 0, detail: 'Update wird heruntergeladen.' });\n      await autoUpdater.downloadUpdate();\n      return { ...value };\n    },\n    async install() {\n      if (value.state !== 'downloaded') throw new Error('Noch kein heruntergeladenes Update verfügbar.');\n      set({ state: 'downloaded', detail: 'FLYXORA wird neu gestartet und aktualisiert.' });\n      await persistBrowserStateSnapshot({ reason: 'update', restoreAll: true });\n      setTimeout(() => autoUpdater.quitAndInstall(true, true), 650);\n      return { ...value };\n    },\n  };\n}\n",
    "after": "    });\n    autoUpdater.on('update-not-available', (info) => set({ state: 'current', percent: 0, releaseName: info?.version || currentVersion, detail: 'FLYXORA ist aktuell.' }));\n    autoUpdater.on('download-progress', (progress) => set({ state: 'downloading', percent: Math.round(progress.percent || 0), detail: `Update wird heruntergeladen: ${Math.round(progress.percent || 0)} %` }));\n    autoUpdater.on('update-downloaded', (info) => set({ state: 'downloaded', percent: 100, releaseName: info?.version || null, releaseNotes: normalizeReleaseNotes(info?.releaseNotes) || value.releaseNotes || '', detail: `Version ${info?.version || ''} ist bereit. Neustart zum Installieren.`.replace(/\\s+/g, ' ').trim() }));\n    autoUpdater.on('error', (error) => set({ state: 'error', installing: false, failedOperation: operation || 'check', percent: 0, detail: `Update ${operation || 'check'} fehlgeschlagen: ${error.message}` }));\n  }\n\n  return {\n    status: () => ({ ...value }),\n    async check() {\n      if (!app.isPackaged || process.platform !== 'win32') return { ...value };\n      if (operation || value.state === 'downloaded') return { ...value };\n      operation = 'check';\n      set({ state: 'checking', detail: 'GitHub Release wird geprüft.' });\n      try { await autoUpdater.checkForUpdates(); }\n      catch (error) { set({ state: 'error', failedOperation: 'check', detail: `Updateprüfung fehlgeschlagen: ${error.message}` }); }\n      finally { operation = null; }\n      return { ...value };\n    },\n    async download() {\n      if (!app.isPackaged || process.platform !== 'win32') return { ...value };\n      if (operation || !(value.state === 'available' || value.state === 'error' && value.failedOperation === 'download')) return { ...value };\n      operation = 'download';\n      set({ state: 'downloading', percent: 0, detail: 'Update wird heruntergeladen.' });\n      try { await autoUpdater.downloadUpdate(); }\n      catch (error) { set({ state: 'error', failedOperation: 'download', detail: `Download fehlgeschlagen: ${error.message}` }); }\n      finally { operation = null; }\n      return { ...value };\n    },\n    async install() {\n      if (operation) return { ...value };\n      if (value.state !== 'downloaded') throw new Error('Noch kein heruntergeladenes Update verfügbar.');\n      operation = 'install';\n      set({ state: 'downloaded', installing: true, detail: 'FLYXORA wird neu gestartet und aktualisiert.' });\n      await persistBrowserStateSnapshot({ reason: 'update', restoreAll: true });\n      setTimeout(() => {\n        try { autoUpdater.quitAndInstall(false, true); }\n        catch (error) { set({ state: 'error', installing: false, failedOperation: 'install', detail: `Installation fehlgeschlagen: ${error.message}` }); }\n        finally { operation = null; }\n      }, 650);\n      return { ...value };\n    },\n  };\n}\n"
  },
  {
    "file": "src/electron-main.mjs",
    "before": "  if (!taxiServer || shutdownStarted) return;\n  event.preventDefault();\n  shutdownStarted = true;\n  Promise.resolve()\n    .then(() => persistBrowserStateSnapshot({ reason: 'shutdown', restoreAll: false }))\n    .catch(() => false)\n    .then(() => taxiServer.close())\n    .finally(() => {\n      taxiServer = null;\n",
    "after": "  if (!taxiServer || shutdownStarted) return;\n  event.preventDefault();\n  shutdownStarted = true;\n  Promise.resolve()\n    .then(() => updateService?.status().installing\n      ? false : persistBrowserStateSnapshot({ reason: 'shutdown', restoreAll: false }))\n    .catch(() => false)\n    .then(() => taxiServer.close())\n    .finally(() => {\n      taxiServer = null;\n"
  },
  {
    "file": "src/simconnect-client.mjs",
    "before": "        this.trafficPoll = null;\n        this.handle = null;\n        this.protocol = null;\n        this.#rejectFacilityRequests(new Error('MSFS-Verbindung wurde getrennt.'));\n        this.engine.setConnection('simConnect', 'disconnected', 'MSFS-Verbindung getrennt');\n        this.retryTimer = setTimeout(() => this.#connect(), this.retryMs);\n      };\n      handle.on('quit', reconnect);\n",
    "after": "        this.trafficPoll = null;\n        this.handle = null;\n        this.protocol = null;\n        this.#rejectFacilityRequests(new Error('MSFS-Verbindung wurde getrennt.'));\n        this.trafficBatch = null;\n        this.engine.setIntegration('simTraffic', { status: 'disconnected', aircraft: [], detail: 'MSFS-Verbindung getrennt', updatedAt: null });\n        this.engine.setConnection('simConnect', 'disconnected', 'MSFS-Verbindung getrennt');\n        this.retryTimer = setTimeout(() => this.#connect(), this.retryMs);\n      };\n      handle.on('quit', reconnect);\n"
  },
  {
    "file": "src/simconnect-client.mjs",
    "before": "    addString('TITLE', SimConnectDataType.STRING128);\n    addString('ATC ID', SimConnectDataType.STRING32);\n\n    handle.on('simObjectDataByType', (received) => {\n      if (received.requestID !== TRAFFIC_DATA_REQUEST) return;\n      if (!this.trafficBatch) this.trafficBatch = { startedAt: Date.now(), aircraft: [] };\n      if (Number(received.outOf) === 0) {\n        this.#publishTrafficBatch();\n        return;\n      }\n",
    "after": "    addString('TITLE', SimConnectDataType.STRING128);\n    addString('ATC ID', SimConnectDataType.STRING32);\n\n    handle.on('simObjectDataByType', (received) => {\n      if (received.requestID !== TRAFFIC_DATA_REQUEST || this.handle !== handle || !this.trafficBatch) return;\n      if (Number(received.outOf) === 0) {\n        this.#publishTrafficBatch();\n        return;\n      }\n"
  },
  {
    "file": "src/simconnect-client.mjs",
    "before": "      } catch (error) {\n        this.engine.setIntegration('simTraffic', { status: 'limited', detail: `Verkehrsobjekt konnte nicht gelesen werden: ${error.message}` });\n      }\n\n      const receivedCount = Number(received.entryNumber ?? 0) + 1;\n      const expectedCount = Number(received.outOf ?? 0);\n      if (expectedCount === 0 || receivedCount >= expectedCount) this.#publishTrafficBatch();\n    });\n\n",
    "after": "      } catch (error) {\n        this.engine.setIntegration('simTraffic', { status: 'limited', detail: `Verkehrsobjekt konnte nicht gelesen werden: ${error.message}` });\n      }\n\n      const receivedCount = Number(received.entryNumber ?? 0);\n      const expectedCount = Number(received.outOf ?? 0);\n      if (expectedCount === 0 || receivedCount >= expectedCount) this.#publishTrafficBatch();\n    });\n\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "        this.discoveryBatch = null;\n        this.detailBatch = null;\n        this.pendingRequests.clear();\n        this.pendingPlanRequests.clear();\n        this.trafficPlanByObjectId.clear();\n        this.retryTimer = setTimeout(() => this.#connect(), this.retryMs);\n      };\n      this.handle.on('quit', reconnect);\n      this.handle.on('close', reconnect);\n",
    "after": "        this.discoveryBatch = null;\n        this.detailBatch = null;\n        this.pendingRequests.clear();\n        this.pendingPlanRequests.clear();\n        this.pendingIdentityRequests.clear();\n        this.trafficPlanByObjectId.clear();\n        this.fallbackAircraft = [];\n        const current = this.engine.publicState().integrations?.simTraffic || {};\n        this.engine.setIntegration('simTraffic', { ...current, aircraft: (current.aircraft || []).filter((entry) => entry.source !== 'simconnect-all') });\n        this.retryTimer = setTimeout(() => this.#connect(), this.retryMs);\n      };\n      this.handle.on('quit', reconnect);\n      this.handle.on('close', reconnect);\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "    } catch {\n      // One malformed object does not invalidate the rest of the discovery batch.\n    }\n\n    const receivedCount = Number(received.entryNumber ?? 0) + 1;\n    if (receivedCount >= expected) this.#finishDiscovery();\n  }\n\n  #finishDiscovery() {\n",
    "after": "    } catch {\n      // One malformed object does not invalidate the rest of the discovery batch.\n    }\n\n    const receivedCount = Number(received.entryNumber ?? 0);\n    if (receivedCount >= expected) this.#finishDiscovery();\n  }\n\n  #finishDiscovery() {\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "    for (const [requestId, pending] of this.pendingRequests) {\n      if (pending.cycle === cycle) this.pendingRequests.delete(requestId);\n    }\n\n    this.fallbackAircraft = batch.aircraft\n      .sort((left, right) => left.callsign.localeCompare(right.callsign, 'en', { numeric: true }))\n      .filter((entry) => !isGenericPassiveAircraft(entry))\n      .slice(0, 180);\n",
    "after": "    for (const [requestId, pending] of this.pendingRequests) {\n      if (pending.cycle === cycle) this.pendingRequests.delete(requestId);\n    }\n\n    this.fallbackUpdatedAt = Date.now();\n    this.fallbackAircraft = batch.aircraft\n      .sort((left, right) => left.callsign.localeCompare(right.callsign, 'en', { numeric: true }))\n      .filter((entry) => !isGenericPassiveAircraft(entry))\n      .slice(0, 180);\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "      const requestId = this.#nextDetailRequestId();\n      this.pendingPlanRequests.set(requestId, objectId);\n      try {\n        this.handle.requestDataOnSimObject(requestId, TRAFFIC_PLAN_DEFINITION, objectId, SimConnectPeriod.ONCE, 0, 0, 0, 0);\n      } catch {\n        this.pendingPlanRequests.delete(requestId);\n      }\n    }\n",
    "after": "      const requestId = this.#nextDetailRequestId();\n      this.pendingPlanRequests.set(requestId, objectId);\n      try {\n        this.handle.requestDataOnSimObject(requestId, TRAFFIC_PLAN_DEFINITION, objectId, SimConnectPeriod.ONCE, 0, 0, 0, 0);\n        setTimeout(() => this.pendingPlanRequests.delete(requestId), 3_000);\n      } catch {\n        this.pendingPlanRequests.delete(requestId);\n      }\n    }\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "    }, delay);\n  }\n\n  #publishMergedTraffic() {\n    const integration = this.engine.publicState().integrations?.simTraffic || {};\n    const currentAircraft = Array.isArray(integration.aircraft) ? integration.aircraft : [];\n    const primary = currentAircraft.filter((entry) => entry?.source !== 'simconnect-all' && !isGenericPassiveAircraft(entry));\n    const usableFallback = this.fallbackAircraft.filter((entry) => !isGenericPassiveAircraft(entry));\n",
    "after": "    }, delay);\n  }\n\n  #publishMergedTraffic() {\n    if (this.stopped || !this.handle) return;\n    const integration = this.engine.publicState().integrations?.simTraffic || {};\n    const currentAircraft = Array.isArray(integration.aircraft) ? integration.aircraft : [];\n    const primary = currentAircraft.filter((entry) => entry?.source !== 'simconnect-all' && !isGenericPassiveAircraft(entry));\n    const usableFallback = this.fallbackAircraft.filter((entry) => !isGenericPassiveAircraft(entry));\n"
  },
  {
    "file": "src/injected-traffic-client.mjs",
    "before": "    });\n  }\n\n  #restoreMergedTrafficIfNeeded() {\n    if (this.stopped || this.fallbackAircraft.length === 0) return;\n    const current = this.engine.publicState().integrations?.simTraffic;\n    const currentById = new Map((Array.isArray(current?.aircraft) ? current.aircraft : [])\n      .map((entry) => [Number(entry?.objectId), entry])\n      .filter(([id]) => Number.isFinite(id)));\n",
    "after": "    });\n  }\n\n  #restoreMergedTrafficIfNeeded() {\n    if (this.stopped || !this.handle || this.fallbackAircraft.length === 0) return;\n    if (Date.now() - (this.fallbackUpdatedAt || 0) > 10_000) { this.fallbackAircraft = []; return; }\n    const current = this.engine.publicState().integrations?.simTraffic;\n    const currentById = new Map((Array.isArray(current?.aircraft) ? current.aircraft : [])\n      .map((entry) => [Number(entry?.objectId), entry])\n      .filter(([id]) => Number.isFinite(id)));\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "      end.edges.push({ ...edgeBase, id: `${feature.id}:${index}:r`, to: start.key });\n    }\n  }\n\n  // OSM parking lead-ins are often drawn as separate ways that stop a few metres before\n  // the taxiway centerline. Join only isolated parking components to their nearest taxiway\n  // node so gates remain routable without inventing shortcuts inside the taxiway network.\n  const visited = new Set();\n  const components = [];\n  for (const node of nodes.values()) {\n    if (visited.has(node.key)) continue;\n    const component = [];\n    const queue = [node];\n    visited.add(node.key);\n    while (queue.length > 0) {\n      const current = queue.pop();\n      component.push(current);\n      for (const edge of current.edges) {\n        if (visited.has(edge.to)) continue;\n        visited.add(edge.to);\n        queue.push(nodes.get(edge.to));\n      }\n    }\n    components.push(component);\n  }\n  const taxiwayNodes = [...nodes.values()].filter((node) => node.kinds.has('taxiway'));\n  let connectorIndex = 0;\n  for (const component of components) {\n    if (component.some((node) => node.kinds.has('taxiway')) || component.length > 80) continue;\n    let closest = null;\n    for (const source of component) {\n      for (const target of taxiwayNodes) {\n        const distance = distanceMeters(source, target);\n        if (distance <= 120 && (!closest || distance < closest.distance)) closest = { source, target, distance };\n      }\n    }\n    if (!closest) continue;\n    const id = `virtual/parking-connector/${connectorIndex++}`;\n    const edgeBase = { distance: closest.distance, ref: '', kind: 'connector', featureId: id, segmentIndex: 0 };\n    closest.source.edges.push({ ...edgeBase, id: `${id}:f`, to: closest.target.key });\n    closest.target.edges.push({ ...edgeBase, id: `${id}:r`, to: closest.source.key });\n  }\n  return { nodes, size: nodes.size };\n}\n\nfunction nearestNodes(graph, point, { limit = 1, maxDistanceMeters = 600 } = {}) {\n",
    "after": "      end.edges.push({ ...edgeBase, id: `${feature.id}:${index}:r`, to: start.key });\n    }\n  }\n\n  // Disconnected scenery geometry must stay disconnected; never invent apron shortcuts.\n  return { nodes, size: nodes.size };\n}\n\nfunction nearestNodes(graph, point, { limit = 1, maxDistanceMeters = 600 } = {}) {\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "  const routes = [];\n  const fingerprints = new Set();\n  const penalizedEdges = new Set();\n  for (let index = 0; index < 3; index += 1) {\n    let result = shortestPath(graph, starts, goals, { ...options, penalizedEdges });\n    if (!result && options.requiredTaxiways?.length && !options.relaxed) {\n      result = shortestPath(graph, starts, goals, { ...options, relaxed: true, penalizedEdges });\n    }\n    if (!result) break;\n    const route = routeFromPath(graph, result, { ...options, index });\n    const fingerprint = route.path.map((point) => `${point.lat.toFixed(5)},${point.lon.toFixed(5)}`).join('|');\n    if (!fingerprints.has(fingerprint)) {\n",
    "after": "  const routes = [];\n  const fingerprints = new Set();\n  const penalizedEdges = new Set();\n  for (let index = 0; index < 3; index += 1) {\n    const result = shortestPath(graph, starts, goals, { ...options, penalizedEdges });\n    if (!result) break;\n    const route = routeFromPath(graph, result, { ...options, index });\n    const fingerprint = route.path.map((point) => `${point.lat.toFixed(5)},${point.lon.toFixed(5)}`).join('|');\n    if (!fingerprints.has(fingerprint)) {\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "  let goals = [];\n\n  if (mode === 'departure') {\n    startPoint = request.start?.type === 'aircraft' ? finitePoint(context.aircraft) : featureAnchor(mapData, request.start);\n    const departureSnapDistance = request.start?.type === 'aircraft' ? 1_200 : 900;\n    starts = nearestNodes(graph, startPoint, { limit: 3, maxDistanceMeters: departureSnapDistance });\n    goals = departureHoldingPoints(mapData, graph, runway);\n    if (request.holdingPoint) goals = goals.filter((entry) => entry.node.key === request.holdingPoint);\n    else if (goals.length > 0) goals = [goals[0]];\n  } else if (mode === 'arrival') {\n    const selectedExit = runwayExitAnchor(mapData, runway, request.runwayExit);\n    starts = selectedExit\n      ? nearestNodes(graph, selectedExit, { limit: 2, maxDistanceMeters: 180 })\n      : runwayAnchorNodes(mapData, graph, runway);\n    startPoint = selectedExit;\n    endPoint = featureAnchor(mapData, request.destination);\n    goals = nearestNodes(graph, endPoint, { limit: 3, maxDistanceMeters: 900 });\n  } else if (mode === 'custom') {\n    startPoint = finitePoint(request.start);\n    endPoint = finitePoint(request.destination);\n    starts = nearestNodes(graph, startPoint, { limit: 3, maxDistanceMeters: 900 });\n    goals = nearestNodes(graph, endPoint, { limit: 3, maxDistanceMeters: 900 });\n  } else {\n    return { routes: [], error: 'Unbekannter Planungsmodus.' };\n  }\n\n",
    "after": "  let goals = [];\n\n  if (mode === 'departure') {\n    startPoint = request.start?.type === 'aircraft' ? finitePoint(context.aircraft) : featureAnchor(mapData, request.start);\n    const departureSnapDistance = 60;\n    starts = nearestNodes(graph, startPoint, { limit: 1, maxDistanceMeters: departureSnapDistance });\n    goals = departureHoldingPoints(mapData, graph, runway);\n    if (request.holdingPoint) goals = goals.filter((entry) => entry.node.key === request.holdingPoint);\n    else if (goals.length > 0) goals = [goals[0]];\n  } else if (mode === 'arrival') {\n    const selectedExit = runwayExitAnchor(mapData, runway, request.runwayExit);\n    starts = selectedExit\n      ? nearestNodes(graph, selectedExit, { limit: 2, maxDistanceMeters: 60 })\n      : runwayAnchorNodes(mapData, graph, runway);\n    startPoint = selectedExit;\n    endPoint = featureAnchor(mapData, request.destination);\n    goals = nearestNodes(graph, endPoint, { limit: 1, maxDistanceMeters: 60 });\n  } else if (mode === 'custom') {\n    startPoint = finitePoint(request.start);\n    endPoint = finitePoint(request.destination);\n    starts = nearestNodes(graph, startPoint, { limit: 1, maxDistanceMeters: 60 });\n    goals = nearestNodes(graph, endPoint, { limit: 1, maxDistanceMeters: 60 });\n  } else {\n    return { routes: [], error: 'Unbekannter Planungsmodus.' };\n  }\n\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "    startPoint,\n    endPoint,\n    source: 'manual',\n  });\n  return { routes, graphNodes: graph.size, mode, runway: runway || null };\n}\n\n// 1.24.8 candidate arrival-to-gate fallback\nexport function deriveTaxiRouteFromClearance(mapData, state) {\n",
    "after": "    startPoint,\n    endPoint,\n    source: 'manual',\n  });\n  return { routes, graphNodes: graph.size, mode, runway: runway || null,\n    error: routes.length ? null : 'Kein durchgängiger Taxiweg in den Kartendaten. Bitte Karte und Freigabe manuell prüfen.' };\n}\n\n// 1.24.8 candidate arrival-to-gate fallback\nexport function deriveTaxiRouteFromClearance(mapData, state) {\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "  const graph = buildTaxiGraph(mapData);\n  if (graph.size < 2) return { routes: [], parsed, error: 'Für diesen Flughafen ist kein routbares Taxiway-Netz verfügbar.' };\n\n  const startPoint = finitePoint(state?.aircraft) ?? finitePoint(state?.gate);\n  const starts = nearestNodes(graph, startPoint, { limit: 3, maxDistanceMeters: 650 });\n  if (starts.length === 0) {\n    return { routes: [], parsed, error: 'Der aktuelle Flugzeugstandort konnte nicht dem Taxiway-Netz zugeordnet werden.' };\n  }\n\n",
    "after": "  const graph = buildTaxiGraph(mapData);\n  if (graph.size < 2) return { routes: [], parsed, error: 'Für diesen Flughafen ist kein routbares Taxiway-Netz verfügbar.' };\n\n  const startPoint = finitePoint(state?.aircraft) ?? finitePoint(state?.gate);\n  const starts = nearestNodes(graph, startPoint, { limit: 1, maxDistanceMeters: 60 });\n  if (starts.length === 0) {\n    return { routes: [], parsed, error: 'Der aktuelle Flugzeugstandort konnte nicht dem Taxiway-Netz zugeordnet werden.' };\n  }\n\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "  let mode = 'departure';\n  if (gatePoint && (explicitGateDestination || taxiInContext)) {\n    mode = 'arrival';\n    endPoint = gatePoint;\n    goals = nearestNodes(graph, gatePoint, { limit: 3, maxDistanceMeters: 650 });\n  } else if (parsed.runway) {\n    goals = runwayAnchorNodes(mapData, graph, parsed.runway);\n  } else {\n    return { routes: [], parsed, error: 'Die Freigabe enthält weder eine Runway noch ein erreichbares Gate-Ziel.' };\n",
    "after": "  let mode = 'departure';\n  if (gatePoint && (explicitGateDestination || taxiInContext)) {\n    mode = 'arrival';\n    endPoint = gatePoint;\n    goals = nearestNodes(graph, gatePoint, { limit: 1, maxDistanceMeters: 60 });\n  } else if (parsed.runway) {\n    goals = runwayAnchorNodes(mapData, graph, parsed.runway);\n  } else {\n    return { routes: [], parsed, error: 'Die Freigabe enthält weder eine Runway noch ein erreichbares Gate-Ziel.' };\n"
  },
  {
    "file": "src/taxi-route-planner.mjs",
    "before": "    endPoint,\n    requiredTaxiways: parsed.taxiways,\n    source: 'clearance-map',\n  });\n  for (const route of routes) {\n    route.label = `Via ${parsed.taxiways.join(' – ')}`;\n    route.taxiways = [...parsed.taxiways];\n  }\n  return { routes, parsed, mode, graphNodes: graph.size };\n}\n",
    "after": "    endPoint,\n    requiredTaxiways: parsed.taxiways,\n    source: 'clearance-map',\n  });\n  return { routes, parsed, mode, graphNodes: graph.size,\n    error: routes.length ? null : 'Die freigegebene Taxiway-Folge ist im Kartennetz nicht durchgängig abbildbar.' };\n}\n"
  },
  {
    "file": "public/live-traffic.js",
    "before": "\nexport const LIVE_TRAFFIC_LIMITS = Object.freeze({\n  groundRadiusNm: 8,\n  arrivingRadiusNm: 80,\n  nearbyRadiusNm: 120,\n  maxRows: 120,\n});\n\nfunction finite(value, fallback = null) {\n",
    "after": "\nexport const LIVE_TRAFFIC_LIMITS = Object.freeze({\n  groundRadiusNm: 8,\n  arrivingRadiusNm: 80,\n  nearbyRadiusNm: 107.9,\n  maxRows: 120,\n});\n\nfunction finite(value, fallback = null) {\n"
  },
  {
    "file": "public/live-traffic.js",
    "before": "  // arrived through the optional AI traffic-plan enrichment is treated as simulator-reported.\n  const reported = Boolean(entry.scheduleEnriched && state);\n  const provenance = { inferred: !reported, distanceNm };\n\n  if (entry.onGround) {\n    if (/shutdown|sleep|parked|parking/.test(state)) return { kind: 'parking', label: 'PARKING', ...provenance };\n    if (/startup|preflight|clearance/.test(state)) return { kind: 'preflight', label: 'PREFLIGHT', ...provenance };\n    if (/push/.test(state)) return { kind: 'pushback', label: 'PUSHBACK', ...provenance };\n    if (/taxi|rollout/.test(state)) return { kind: 'taxi', label: 'TAXI', ...provenance };\n    if (groundSpeed <= 2) return { kind: 'parking', label: 'PARKING', inferred: true, distanceNm };\n    return { kind: 'taxi', label: 'TAXI', inferred: true, distanceNm };\n  }\n\n  // Enriched traffic-state data is authoritative. Plain SimConnect traffic often receives a\n  // synthesized \"enroute\" state, so movement/altitude must be evaluated before that fallback.\n",
    "after": "  // arrived through the optional AI traffic-plan enrichment is treated as simulator-reported.\n  const reported = Boolean(entry.scheduleEnriched && state);\n  const provenance = { inferred: !reported, distanceNm };\n\n  if (entry.onGround && !reported) return { kind: groundSpeed <= 2 ? 'parking' : 'taxi', label: groundSpeed <= 2 ? 'STOPPED' : 'GROUND MOVING', inferred: true, distanceNm };\n  if (entry.onGround) {\n    if (/shutdown|sleep|parked|parking/.test(state)) return { kind: 'parking', label: 'PARKING', ...provenance };\n    if (/startup|preflight|clearance/.test(state)) return { kind: 'preflight', label: 'PREFLIGHT', ...provenance };\n    if (/push/.test(state)) return { kind: 'pushback', label: 'PUSHBACK', ...provenance };\n    if (/taxi|rollout/.test(state)) return { kind: 'taxi', label: 'TAXI', ...provenance };\n    if (groundSpeed <= 2) return { kind: 'parking', label: 'STOPPED', inferred: true, distanceNm };\n    return { kind: 'taxi', label: 'GROUND MOVING', inferred: true, distanceNm };\n  }\n\n  // Enriched traffic-state data is authoritative. Plain SimConnect traffic often receives a\n  // synthesized \"enroute\" state, so movement/altitude must be evaluated before that fallback.\n"
  },
  {
    "file": "public/live-traffic.js",
    "before": "    && distanceNm <= LIVE_TRAFFIC_LIMITS.arrivingRadiusNm\n    && verticalSpeed <= -100\n    && altitude > 0\n    && altitude <= 15_000;\n  if (plausiblyArriving) return { kind: /landing/.test(state) ? 'landing' : 'arriving', label: /landing/.test(state) ? 'LANDING' : 'ARRIVING', inferred: true, distanceNm };\n  if (/landing/.test(state)) return { kind: 'landing', label: 'LANDING', inferred: true, distanceNm };\n  if (/approach/.test(state)) return { kind: 'arriving', label: 'ARRIVING', inferred: true, distanceNm };\n  if (/takeoff|depart|climb/.test(state) || verticalSpeed >= 500 && altitude < 18_000) return { kind: 'climb', label: 'CLIMB', inferred: true, distanceNm };\n  if (/enroute|cruise|simple flight|flt plan|waypoint|pattern/.test(state)) return { kind: 'enroute', label: 'ENROUTE', inferred: true, distanceNm };\n  return { kind: 'airborne', label: 'AIRBORNE', inferred: true, distanceNm };\n}\n\nfunction within(distanceNm, radiusNm) {\n  return distanceNm === null || distanceNm <= radiusNm;\n}\n\nexport function buildLiveTrafficModel(entries = [], ownship = {}, view = 'nearby') {\n  const normalizedView = ['ground', 'arriving', 'nearby'].includes(view) ? view : 'nearby';\n",
    "after": "    && distanceNm <= LIVE_TRAFFIC_LIMITS.arrivingRadiusNm\n    && verticalSpeed <= -100\n    && altitude > 0\n    && altitude <= 15_000;\n  if (plausiblyArriving) return { kind: 'arriving', label: 'DESCENDING', inferred: true, distanceNm };\n\n\n  if (verticalSpeed >= 500) return { kind: 'climb', label: 'CLIMB', inferred: true, distanceNm };\n\n  return { kind: 'airborne', label: 'AIRBORNE', inferred: true, distanceNm };\n}\n\nfunction within(distanceNm, radiusNm) {\n  return Number.isFinite(distanceNm) && distanceNm <= radiusNm;\n}\n\nexport function buildLiveTrafficModel(entries = [], ownship = {}, view = 'nearby') {\n  const normalizedView = ['ground', 'arriving', 'nearby'].includes(view) ? view : 'nearby';\n"
  },
  {
    "file": "scripts/test-live-traffic.mjs",
    "before": "const regional = { objectId: 8, lat: 52.5, lon: 9.0, onGround: false, groundSpeed: 430, altitudeFeet: 26000, verticalSpeedFpm: 0, state: 'enroute', callsign: 'BAW120NM', title: 'B737' };\nconst farAway = { objectId: 5, lat: 54.0, lon: 10.5, onGround: true, groundSpeed: 0, state: 'parked', callsign: 'AIGFAR', title: 'AIGAI_B737' };\nconst passive = { objectId: 10, lat: 51.29, lon: 6.77, onGround: true, groundSpeed: 0, state: 'parked', callsign: 'AS-GEN', atcId: 'AS-GEN', title: 'Asobo PassiveAircraft Generic' };\n\nassert.equal(classifyLiveTraffic(ground, ownship).label, 'PARKING');\nassert.equal(classifyLiveTraffic(ground, ownship).inferred, true);\nassert.equal(classifyLiveTraffic(taxi, ownship).label, 'TAXI');\nassert.equal(classifyLiveTraffic(arriving, ownship).label, 'ARRIVING');\nassert.equal(classifyLiveTraffic(syntheticEnrouteArrival, ownship).label, 'ARRIVING', 'synthesized enroute state must not hide a descending arrival');\nassert.equal(classifyLiveTraffic(cruise, ownship).label, 'ENROUTE');\nassert.equal(classifyLiveTraffic(cruise, ownship).inferred, true, 'reader-synthesized state must remain marked inferred');\nassert.equal(classifyLiveTraffic(reportedApproach, ownship).inferred, false, 'AI traffic-plan state should be marked reported');\nassert.equal(isGenericPassiveTraffic(passive), true, 'generic Asobo PassiveAircraft placeholders must be filtered');\nassert.equal(isGenericPassiveTraffic({ ...passive, atcId: 'DLH123', callsign: 'DLH123' }), false, 'a real traffic identity must not be filtered just because the model is passive');\n",
    "after": "const regional = { objectId: 8, lat: 52.5, lon: 9.0, onGround: false, groundSpeed: 430, altitudeFeet: 26000, verticalSpeedFpm: 0, state: 'enroute', callsign: 'BAW120NM', title: 'B737' };\nconst farAway = { objectId: 5, lat: 54.0, lon: 10.5, onGround: true, groundSpeed: 0, state: 'parked', callsign: 'AIGFAR', title: 'AIGAI_B737' };\nconst passive = { objectId: 10, lat: 51.29, lon: 6.77, onGround: true, groundSpeed: 0, state: 'parked', callsign: 'AS-GEN', atcId: 'AS-GEN', title: 'Asobo PassiveAircraft Generic' };\n\nassert.equal(classifyLiveTraffic(ground, ownship).label, 'STOPPED');\nassert.equal(classifyLiveTraffic(ground, ownship).inferred, true);\nassert.equal(classifyLiveTraffic(taxi, ownship).label, 'GROUND MOVING');\nassert.equal(classifyLiveTraffic(arriving, ownship).label, 'DESCENDING');\nassert.equal(classifyLiveTraffic(syntheticEnrouteArrival, ownship).label, 'DESCENDING', 'synthesized enroute state must not hide a descending arrival');\nassert.equal(classifyLiveTraffic(cruise, ownship).label, 'AIRBORNE');\nassert.equal(classifyLiveTraffic(cruise, ownship).inferred, true, 'reader-synthesized state must remain marked inferred');\nassert.equal(classifyLiveTraffic(reportedApproach, ownship).inferred, false, 'AI traffic-plan state should be marked reported');\nassert.equal(isGenericPassiveTraffic(passive), true, 'generic Asobo PassiveAircraft placeholders must be filtered');\nassert.equal(isGenericPassiveTraffic({ ...passive, atcId: 'DLH123', callsign: 'DLH123' }), false, 'a real traffic identity must not be filtered just because the model is passive');\n"
  },
  {
    "file": "scripts/test-live-traffic.mjs",
    "before": "assert.equal(trafficAircraftLabel({ title: 'AIGAM SunExpress Boeing 737-800' }), 'B737-800');\n\nconst model = buildLiveTrafficModel([ground, taxi, arriving, syntheticEnrouteArrival, cruise, longApproach, regional, farAway, passive], ownship, 'nearby');\nassert.equal(model.limits.arrivingRadiusNm, 80);\nassert.equal(model.limits.nearbyRadiusNm, 120);\nassert.equal(model.limits.maxRows, 120);\nassert.equal(model.counts.ground, 2, 'far-away parked and generic passive traffic must not pollute the airport ground view');\nassert.equal(model.counts.arriving, 3, 'Arriving should include plausible descending traffic out to 80 NM, including synthesized enroute state');\nassert.equal(model.counts.nearby, 7, 'Nearby should include regional traffic but exclude generic PassiveAircraft placeholders');\nassert.deepEqual(model.rows.map((entry) => entry.objectId), [1, 2, 3, 4, 9, 7, 8], 'nearby rows should be distance sorted');\n\nconst groundModel = buildLiveTrafficModel([ground, taxi, arriving, cruise, farAway, passive], ownship, 'ground');\nassert.deepEqual(groundModel.rows.map((entry) => entry.objectId), [1, 2]);\n\n",
    "after": "assert.equal(trafficAircraftLabel({ title: 'AIGAM SunExpress Boeing 737-800' }), 'B737-800');\n\nconst model = buildLiveTrafficModel([ground, taxi, arriving, syntheticEnrouteArrival, cruise, longApproach, regional, farAway, passive], ownship, 'nearby');\nassert.equal(model.limits.arrivingRadiusNm, 80);\nassert.equal(model.limits.nearbyRadiusNm, 107.9);\nassert.equal(model.limits.maxRows, 120);\nassert.equal(model.counts.ground, 2, 'far-away parked and generic passive traffic must not pollute the airport ground view');\nassert.equal(model.counts.arriving, 3, 'Arriving should include plausible descending traffic out to 80 NM, including synthesized enroute state');\nassert.equal(model.counts.nearby, 6, 'Nearby should include regional traffic but exclude generic PassiveAircraft placeholders');\nassert.deepEqual(model.rows.map((entry) => entry.objectId), [1, 2, 3, 4, 9, 7], 'nearby rows should be distance sorted');\n\nconst groundModel = buildLiveTrafficModel([ground, taxi, arriving, cruise, farAway, passive], ownship, 'ground');\nassert.deepEqual(groundModel.rows.map((entry) => entry.objectId), [1, 2]);\n\n"
  },
  {
    "file": "public/index.html",
    "before": "          <header class=\"page-heading live-traffic-heading\"><div><small>SIMCONNECT · LOCAL TRAFFIC</small><h1>Live Traffic</h1><p>Observed simulator traffic near your aircraft. No invented schedules, destinations or airport-board data.</p></div><span id=\"flightboard-status-pill\" class=\"module-status waiting\">WAITING</span></header>\n          <div class=\"flightboard-layout live-traffic-layout\">\n            <article class=\"efb-card flightboard-card live-traffic-card\">\n              <header class=\"flightboard-toolbar live-traffic-toolbar\">\n                <div><small>LOCAL TRAFFIC</small><strong id=\"flightboard-airport\">LIVE TRAFFIC</strong><span id=\"live-traffic-scope\" class=\"live-traffic-scope\">120 NM · MAX 40</span></div>\n                <div class=\"flightboard-tabs live-traffic-tabs\" role=\"group\" aria-label=\"Live Traffic view\">\n                  <button type=\"button\" data-traffic-view=\"ground\"><span>GROUND</span><b id=\"flightboard-ground-count\">0</b></button>\n                  <button type=\"button\" data-traffic-view=\"arriving\"><span>ARRIVING</span><b id=\"flightboard-arriving-count\">0</b></button>\n                  <button type=\"button\" class=\"active\" data-traffic-view=\"nearby\"><span>NEARBY</span><b id=\"flightboard-nearby-count\">0</b></button>\n                </div>\n                <button id=\"flightboard-refresh\" class=\"secondary-card-action\" type=\"button\" data-i18n=\"refresh\">REFRESH</button>\n              </header>\n              <div class=\"live-traffic-trust-note\"><strong>WHAT THIS SHOWS</strong><span>Position, movement, aircraft identity and simulator-reported state. ARRIVING may be inferred from distance, altitude and descent when MSFS does not publish a traffic state.</span></div>\n              <div class=\"flightboard-table live-traffic-table\" role=\"table\" aria-label=\"Local simulator live traffic\">\n                <div class=\"flightboard-row flightboard-head live-traffic-row\" role=\"row\"><span>TRAFFIC</span><span>AIRCRAFT</span><span>POSITION</span><span>ALT / GS</span><span>DISTANCE</span><span>STATUS</span></div>\n                <div id=\"flightboard-list\"><p class=\"empty-list\">No local simulator traffic received yet.</p></div>\n              </div>\n",
    "after": "          <header class=\"page-heading live-traffic-heading\"><div><small>SIMCONNECT · LOCAL TRAFFIC</small><h1>Live Traffic</h1><p>Observed simulator traffic near your aircraft. No invented schedules, destinations or airport-board data.</p></div><span id=\"flightboard-status-pill\" class=\"module-status waiting\">WAITING</span></header>\n          <div class=\"flightboard-layout live-traffic-layout\">\n            <article class=\"efb-card flightboard-card live-traffic-card\">\n              <header class=\"flightboard-toolbar live-traffic-toolbar\">\n                <div><small>LOCAL TRAFFIC</small><strong id=\"flightboard-airport\">LIVE TRAFFIC</strong><span id=\"live-traffic-scope\" class=\"live-traffic-scope\">107.9 NM · MAX 120</span></div>\n                <div class=\"flightboard-tabs live-traffic-tabs\" role=\"group\" aria-label=\"Live Traffic view\">\n                  <button type=\"button\" data-traffic-view=\"ground\"><span>GROUND</span><b id=\"flightboard-ground-count\">0</b></button>\n                  <button type=\"button\" data-traffic-view=\"arriving\"><span>DESCENT / APPROACH</span><b id=\"flightboard-arriving-count\">0</b></button>\n                  <button type=\"button\" class=\"active\" data-traffic-view=\"nearby\"><span>NEARBY</span><b id=\"flightboard-nearby-count\">0</b></button>\n                </div>\n                <button id=\"flightboard-refresh\" class=\"secondary-card-action\" type=\"button\" data-i18n=\"refresh\">REFRESH</button>\n              </header>\n              <div class=\"live-traffic-trust-note\"><strong>WHAT THIS SHOWS</strong><span>Position, movement, aircraft identity and simulator-reported state. DESCENDING only describes movement; it does not confirm an arrival at this airport. Range is limited to 200 km around your aircraft; only objects exposed by MSFS are shown.</span></div>\n              <div class=\"flightboard-table live-traffic-table\" role=\"table\" aria-label=\"Local simulator live traffic\">\n                <div class=\"flightboard-row flightboard-head live-traffic-row\" role=\"row\"><span>TRAFFIC</span><span>AIRCRAFT</span><span>POSITION</span><span>ALT / GS</span><span>DISTANCE</span><span>STATUS</span></div>\n                <div id=\"flightboard-list\"><p class=\"empty-list\">No local simulator traffic received yet.</p></div>\n              </div>\n"
  },
  {
    "file": "public/service-worker.js",
    "before": "const CACHE_NAME = 'flyxora-v1.24.13-news-removed-quit-on-close';\n",
    "after": "const CACHE_NAME = 'flyxora-v1.24.13-news-removed-quit-on-close-reliability';\n"
  },
  {
    "file": "src/server.mjs",
    "before": "      await accessManager.stop();\n      await simSession.stop();\n      await lanDiscovery?.stop();\n      for (const client of sseClients) client.end();\n      await new Promise((resolve) => server.close(resolve));\n    },\n  };\n}\n\n",
    "after": "      await accessManager.stop();\n      await simSession.stop();\n      await lanDiscovery?.stop();\n      for (const client of sseClients) client.end();\n      await new Promise((resolve) => {\n        server.close(resolve);\n        server.closeAllConnections();\n      });\n    },\n  };\n}\n\n"
  }
]
